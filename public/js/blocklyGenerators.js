define(['./commons',
        'blockly.javascript'], function(){
  

  Blockly.JavaScript['evolConf'] = function(block){

    /*var mainInit = block.getChildren().map(function(childBlock){
      var code = Blockly.Javascript.blockToCode(childBlock)
      return Array.isArray(code) ? null : code;
    }).join("");


    var codeParts = [];

    var code = codeParts.filter(function(x){return x;}).join("\n");

    return code;*/
    return "fake";
  };

});
